package objects;

import javafx.scene.paint.Color;
import javafx.scene.shape.Circle;

public class Projectile extends Circle{
	public Projectile(double x, double y, double radius, Color color){
		super(x, y, radius, color);
	}
}
